[pytest]
markers =
    integration: marks integration tests. Have to be enabled explicitly with -m integration

python_files = test_*.py
# Disable integration tests by default
addopts = -m "not integration"